[project]
name = "bug-triage-env"
version = "0.1.0"
description = "OpenEnv Bug Triage Environment"
authors = [
  { name = "Deekshitha" }
]

dependencies = [
  "fastapi",
  "uvicorn",
  "openenv>=0.2.0"
]

[project.scripts]
server = "server.app:main"
